// === MemoDrops Discovery Dashboard: Config ===
// Ajuste as URLs das APIs (Harvester e IC Engine)
window.MEMODROPS_CONFIG = {
  HARVESTER_BASE_URL: window.location.origin, // Mesma URL do backoffice
  IC_ENGINE_BASE_URL: window.location.origin, // Mesma URL do backoffice
  DEFAULT_WINDOW: "12m"
};
