[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node dist/server-test-v5.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[deploy.volumes]]
mountPath = "/data"
name = "uploads-volume"
